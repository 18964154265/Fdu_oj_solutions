#include<stdio.h>
#define N 100000
int a[N];
int main()
{int n,sum=0;
 scanf("%d",&n);a[1]=2;
 for(int i=1;i<=n;i++)
 {a[i+1]=a[i]*2;
  sum+=a[i]; }
 printf("%d\n",sum);
for(int i=1;i<=n;i++)
  printf("Day %d: %d\n",i,a[n+1-i]);
return 0;
}
